# GOGS Go Git Server running on Zeta
-----------
This package installs a git server written in go on Zeta

#### Future Work
- Use Network/Container linking to run the container behind an HTTPS Server (NGINX?)

